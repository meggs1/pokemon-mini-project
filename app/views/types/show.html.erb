<h1><%= @type.name %></h1>

<% @type.pokemons.each do |pokemon| %>
    <p><%= link_to pokemon.name, pokemon %></p>
<%end%>

<%= link_to "Edit Type", edit_type_path(@type) %>
<%= link_to "Delete Type", @type, method: "delete" %>